using Gtk 4.0;
using Adw 1;

template $ScrptWindow: Adw.ApplicationWindow {
  title: _("Scriptorium");

  content: Adw.NavigationView navigation {
    //$ScrptLibraryView library {}

    //$ScrptEditorView editor {}

    popped => $on_navigationview_popped();
  };
}

